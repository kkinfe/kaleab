import { ArticleLayout } from "@/components/BlogLayout";

export const meta = {
  author: "Kaleab Kinfe",
  date: "2022-04-12",
  title: "Developing on your local computer is dead",
  description: "Is it finally convenient to dump localhost development?",
};

export default (props) => <ArticleLayout meta={meta} {...props} />;
